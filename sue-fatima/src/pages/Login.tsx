import { FormEvent, useState } from 'react';
import { ArrowRight, LockKeyhole, ShieldCheck } from 'lucide-react';
import { useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '@/context/AuthContext';

export default function Login() { const { login } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); setError(''); setBusy(true); try { await login(email, password); navigate((location.state as { from?: { pathname?: string } } | null)?.from?.pathname ?? '/', { replace: true }); } catch { setError('El email o la contrasena no son correctos.'); } finally { setBusy(false); } }; return <main className="login-page"><div className="login-visual"><div className="visual-brand"><span className="visual-mark">S.U.E.</span><span>Sistema Unificado<br />de Emergencias Escolares</span></div><div className="visual-copy"><div className="pulse-ring"><ShieldCheck size={28} /></div><p>Coordinacion en tiempo real</p><h1>Seguridad que<br /><em>nos une.</em></h1><span>Una institucion preparada responde mejor.</span></div><div className="visual-foot">Instituto Tecnico<br /><strong>Nuestra Senora de Fatima</strong></div></div><div className="login-card-wrap"><div className="login-card"><div className="mobile-logo">S.U.E.</div><h2>Bienvenido/a</h2><p className="login-subtitle">Ingresa para gestionar los simulacros de evacuacion.</p><form onSubmit={submit}><label>Email institucional<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="nombre@fatima.edu.ar" required /></label><label>Contrasena<div className="password-wrap"><input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="Ingresa tu contrasena" required /><LockKeyhole size={17} /></div></label>{error && <div className="login-error">{error}</div>}<button className="login-button" disabled={busy}>{busy ? 'Ingresando...' : 'Ingresar'} {!busy && <ArrowRight size={18} />}</button></form><div className="login-note"><ShieldCheck size={17} /> Acceso seguro y protegido</div></div><div className="login-copyright">(c) 2026 Instituto Tecnico Nuestra Senora de Fatima</div></div></main>; }
